## ideas to create image and then use it in rules_oci
# create local docker registry
# docker run -d -p 5000:5000 --name registry registry:2

# docker build static_server -f infra/images/static_servcer.docker
